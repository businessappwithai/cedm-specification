entity:
  name: Organization
  namespace: cedm.domain
  kind: party_role
  description: An organization is an identifiable structured group of people or legal entities that participates in business
    activities. It may represent a legal entity, company, business unit, department, branch, or other organizational unit.
  identity:
    key: organizationId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: organizationId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the organization record associated with this Organization. The identifier connects this record to
        the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: code
    type: string
    required: true
    unique: true
    maxLength: 50
    help:
      summary: A human-readable business code used to identify or reference the record in operational processes and integrations.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: name
    type: string
    required: true
    maxLength: 200
    help:
      summary: The human-readable name used by people, reports, searches, and related business processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: organizationType
    type: enum
    required: true
    values:
    - ENTERPRISE
    - COMPANY
    - BUSINESS_UNIT
    - DIVISION
    - DEPARTMENT
    - BRANCH
    - SUBSIDIARY
    - OTHER
    help:
      summary: Captures the business meaning of organization type for the Organization. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        ENTERPRISE: Represents the enterprise state or classification in the context of Organization.
        COMPANY: Represents the company state or classification in the context of Organization.
        BUSINESS_UNIT: Represents the business unit state or classification in the context of Organization.
        DIVISION: Represents the division state or classification in the context of Organization.
        DEPARTMENT: Represents the department state or classification in the context of Organization.
        BRANCH: Represents the branch state or classification in the context of Organization.
        SUBSIDIARY: Represents the subsidiary state or classification in the context of Organization.
        OTHER: Represents the other state or classification in the context of Organization.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - DRAFT
    - ACTIVE
    - INACTIVE
    - RETIRED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        DRAFT: Represents the draft state or classification in the context of Organization.
        ACTIVE: Represents the active state or classification in the context of Organization.
        INACTIVE: Represents the inactive state or classification in the context of Organization.
        RETIRED: Represents the retired state or classification in the context of Organization.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: legalName
    type: string
    required: false
    maxLength: 300
    help:
      summary: Captures the business meaning of legal name for the Organization. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: registrationNumber
    type: string
    required: false
    maxLength: 100
    help:
      summary: Captures the business meaning of registration number for the Organization. It is interpreted together with
        the entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: taxIdentifier
    type: string
    required: false
    maxLength: 100
    help:
      summary: Captures the business meaning of tax identifier for the Organization. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: createdAt
    type: datetime
    required: true
    systemManaged: true
    help:
      summary: The system-managed timestamp recording when the record was created. It supports auditability, chronology, and
        operational reporting.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: createdBy
    type: reference
    target: Party
    required: false
    systemManaged: true
    help:
      summary: The party or user responsible for creating the record. It links the record to its audit actor and should not
        normally be supplied as ordinary business data.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: This field connects Organization to Party. The reference establishes business context between the
        two entities and lets processes navigate from this record to the related Party.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: updatedAt
    type: datetime
    required: true
    systemManaged: true
    help:
      summary: The system-managed timestamp recording the latest modification of the record. It supports synchronization,
        audit, and change detection.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: Its meaning is specific to Organization; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: updatedBy
    type: reference
    target: Party
    required: false
    systemManaged: true
    help:
      summary: The party or user responsible for the latest modification. It provides the audit link for the most recent change.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Organization records, where
        applicable.
      relationshipContext: This field connects Organization to Party. The reference establishes business context between the
        two entities and lets processes navigate from this record to the related Party.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: parentOrganization
    target: Organization
    cardinality: 0..1
    ownership: reference
    description: Immediate parent organizational unit.
    help:
      summary: Connects Organization to Organization so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Organization records associated with a Organization.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: childOrganizations
    target: Organization
    cardinality: 0..*
    ownership: reference
    inverse: parentOrganization
    help:
      summary: Connects Organization to Organization so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Organization records associated with a Organization.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: party
    target: Party
    cardinality: 0..1
    ownership: reference
    description: Party identity represented by this organization.
    help:
      summary: Connects Organization to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a Organization.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: addresses
    target: Address
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects Organization to Address so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Address records associated with a Organization.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: locations
    target: Location
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects Organization to Location so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Location records associated with a Organization.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: ORG-001
    rule: An organization cannot be its own parent.
  - id: ORG-002
    rule: The organizational hierarchy must not contain cycles.
  - id: ORG-003
    rule: code must be unique within its ERP/business context.
  lifecycle:
    states:
    - DRAFT
    - ACTIVE
    - INACTIVE
    - RETIRED
  audit:
    enabled: true
    trackCreatedBy: true
    trackCreatedAt: true
    trackUpdatedBy: true
    trackUpdatedAt: true
  tags:
  - organization
  - party
  - hierarchy
  - master-data
  help:
    summary: Represents a party role called Organization within the CEDM business model.
    businessMeaning: Organization is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate Organization records.
    relationshipContext: The entity participates in a wider business graph through relationships with Organization, Organization,
      Party, Address, Location. These relationships provide the context needed to interpret the record rather than treating
      its fields as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical Organization record represents one identifiable business occurrence or master-data object that can
      be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-ORGANIZATION

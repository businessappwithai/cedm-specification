entity:
  name: Nonconformance
  namespace: cedm.domain
  kind: quality_issue
  description: A recorded deviation from a requirement, specification, procedure, or expected result.
  identity:
    key: nonconformanceId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: nonconformanceId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the nonconformance record associated with this Nonconformance. The identifier connects this record
        to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Nonconformance records, where
        applicable.
      relationshipContext: Its meaning is specific to Nonconformance; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: number
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of number for the Nonconformance. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Nonconformance records, where
        applicable.
      relationshipContext: Its meaning is specific to Nonconformance; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: severity
    type: enum
    required: true
    values:
    - LOW
    - MEDIUM
    - HIGH
    - CRITICAL
    help:
      summary: Captures the business meaning of severity for the Nonconformance. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Nonconformance records, where
        applicable.
      relationshipContext: Its meaning is specific to Nonconformance; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        LOW: Represents the low state or classification in the context of Nonconformance.
        MEDIUM: Represents the medium state or classification in the context of Nonconformance.
        HIGH: Represents the high state or classification in the context of Nonconformance.
        CRITICAL: Represents the critical state or classification in the context of Nonconformance.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: OPEN
    values:
    - OPEN
    - UNDER_REVIEW
    - CONTAINED
    - CORRECTIVE_ACTION
    - CLOSED
    - REJECTED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Nonconformance records, where
        applicable.
      relationshipContext: Its meaning is specific to Nonconformance; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        OPEN: Represents the open state or classification in the context of Nonconformance.
        UNDER_REVIEW: Represents the under review state or classification in the context of Nonconformance.
        CONTAINED: Represents the contained state or classification in the context of Nonconformance.
        CORRECTIVE_ACTION: Represents the corrective action state or classification in the context of Nonconformance.
        CLOSED: Represents the closed state or classification in the context of Nonconformance.
        REJECTED: Represents the rejected state or classification in the context of Nonconformance.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: description
    type: string
    required: true
    maxLength: 4000
    help:
      summary: A business description that explains the purpose, scope, or meaning of the record to users and downstream processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Nonconformance records, where
        applicable.
      relationshipContext: Its meaning is specific to Nonconformance; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: detectedAt
    type: datetime
    required: true
    help:
      summary: Records when the detected event occurred. It establishes chronology, supports auditability, and helps coordinate
        related lifecycle and process activities.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Nonconformance records, where
        applicable.
      relationshipContext: Its meaning is specific to Nonconformance; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: closedAt
    type: datetime
    required: false
    help:
      summary: Records when the closed event occurred. It establishes chronology, supports auditability, and helps coordinate
        related lifecycle and process activities.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Nonconformance records, where
        applicable.
      relationshipContext: Its meaning is specific to Nonconformance; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: inspection
    target: QualityInspection
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Nonconformance to QualityInspection so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about QualityInspection records associated with a Nonconformance.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: product
    target: Product
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Nonconformance to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a Nonconformance.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: owner
    target: Party
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Nonconformance to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a Nonconformance.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: correctiveActions
    target: CorrectiveAction
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects Nonconformance to CorrectiveAction so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about CorrectiveAction records associated with a Nonconformance.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: NONCONFORMANCE-001
    rule: closedAt must be present when status is CLOSED.
  help:
    summary: Represents a quality issue called Nonconformance within the CEDM business model.
    businessMeaning: Nonconformance is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate Nonconformance records.
    relationshipContext: The entity participates in a wider business graph through relationships with QualityInspection, Product,
      Party, CorrectiveAction. These relationships provide the context needed to interpret the record rather than treating
      its fields as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical Nonconformance record represents one identifiable business occurrence or master-data object that can
      be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-NONCONFORMANCE

entity:
  name: ChemicalBatch
  namespace: cedm.domain
  kind: process_industry_entity
  description: A traceable batch of chemical or process-industry material produced, received, or processed under controlled
    conditions.
  identity:
    key: chemicalBatchId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: chemicalBatchId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the chemical batch record associated with this ChemicalBatch. The identifier connects this record
        to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ChemicalBatch records, where
        applicable.
      relationshipContext: Its meaning is specific to ChemicalBatch; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: batchNumber
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of batch number for the ChemicalBatch. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ChemicalBatch records, where
        applicable.
      relationshipContext: Its meaning is specific to ChemicalBatch; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: manufactureDate
    type: date
    required: false
    help:
      summary: Records the business date associated with the manufacture. It is used in chronology, eligibility, scheduling,
        reporting, and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ChemicalBatch records, where
        applicable.
      relationshipContext: Its meaning is specific to ChemicalBatch; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: expiryDate
    type: date
    required: false
    help:
      summary: Records the business date associated with the expiry. It is used in chronology, eligibility, scheduling, reporting,
        and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ChemicalBatch records, where
        applicable.
      relationshipContext: Its meaning is specific to ChemicalBatch; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: status
    type: enum
    required: true
    default: QUARANTINED
    values:
    - PLANNED
    - QUARANTINED
    - RELEASED
    - REJECTED
    - EXPIRED
    - CONSUMED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ChemicalBatch records, where
        applicable.
      relationshipContext: Its meaning is specific to ChemicalBatch; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        PLANNED: Represents the planned state or classification in the context of ChemicalBatch.
        QUARANTINED: Represents the quarantined state or classification in the context of ChemicalBatch.
        RELEASED: Represents the released state or classification in the context of ChemicalBatch.
        REJECTED: Represents the rejected state or classification in the context of ChemicalBatch.
        EXPIRED: Represents the expired state or classification in the context of ChemicalBatch.
        CONSUMED: Represents the consumed state or classification in the context of ChemicalBatch.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: product
    target: Product
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects ChemicalBatch to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a ChemicalBatch.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: location
    target: Location
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects ChemicalBatch to Location so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Location records associated with a ChemicalBatch.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: qualityInspections
    target: QualityInspection
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects ChemicalBatch to QualityInspection so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about QualityInspection records associated with a ChemicalBatch.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: CHEMICAL-BATCH-001
    rule: expiryDate must not precede manufactureDate.
  help:
    summary: Represents a process industry entity called ChemicalBatch within the CEDM business model.
    businessMeaning: ChemicalBatch is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate ChemicalBatch records.
    relationshipContext: The entity participates in a wider business graph through relationships with Product, Location, QualityInspection.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical ChemicalBatch record represents one identifiable business occurrence or master-data object that can
      be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-CHEMICAL-BATCH

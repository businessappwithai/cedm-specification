entity:
  name: MiningSite
  namespace: cedm.domain
  kind: mining_entity
  description: A geographic site at which mineral or natural-resource extraction activities occur.
  identity:
    key: miningSiteId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: miningSiteId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the mining site record associated with this MiningSite. The identifier connects this record to the
        corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating MiningSite records, where
        applicable.
      relationshipContext: Its meaning is specific to MiningSite; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: siteCode
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of site code for the MiningSite. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating MiningSite records, where
        applicable.
      relationshipContext: Its meaning is specific to MiningSite; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: name
    type: string
    required: true
    maxLength: 300
    help:
      summary: The human-readable name used by people, reports, searches, and related business processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating MiningSite records, where
        applicable.
      relationshipContext: Its meaning is specific to MiningSite; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: resourceType
    type: string
    required: true
    maxLength: 100
    help:
      summary: Captures the business meaning of resource type for the MiningSite. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating MiningSite records, where
        applicable.
      relationshipContext: Its meaning is specific to MiningSite; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - EXPLORATION
    - DEVELOPMENT
    - ACTIVE
    - SUSPENDED
    - CLOSED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating MiningSite records, where
        applicable.
      relationshipContext: Its meaning is specific to MiningSite; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        EXPLORATION: Represents the exploration state or classification in the context of MiningSite.
        DEVELOPMENT: Represents the development state or classification in the context of MiningSite.
        ACTIVE: Represents the active state or classification in the context of MiningSite.
        SUSPENDED: Represents the suspended state or classification in the context of MiningSite.
        CLOSED: Represents the closed state or classification in the context of MiningSite.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: location
    target: Location
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects MiningSite to Location so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Location records associated with a MiningSite.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: organization
    target: Organization
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects MiningSite to Organization so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Organization records associated with a MiningSite.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: productionRecords
    target: ProductionRecord
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects MiningSite to ProductionRecord so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about ProductionRecord records associated with a MiningSite.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  help:
    summary: Represents a mining entity called MiningSite within the CEDM business model.
    businessMeaning: MiningSite is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate MiningSite records.
    relationshipContext: The entity participates in a wider business graph through relationships with Location, Organization,
      ProductionRecord. These relationships provide the context needed to interpret the record rather than treating its fields
      as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical MiningSite record represents one identifiable business occurrence or master-data object that can be
      referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-MINING-SITE

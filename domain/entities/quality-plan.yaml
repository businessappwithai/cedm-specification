entity:
  name: QualityPlan
  namespace: cedm.domain
  kind: quality_definition
  description: A defined set of inspections, tests, sampling rules, and acceptance criteria.
  identity:
    key: qualityPlanId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: qualityPlanId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the quality plan record associated with this QualityPlan. The identifier connects this record to
        the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating QualityPlan records, where
        applicable.
      relationshipContext: Its meaning is specific to QualityPlan; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: code
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: A human-readable business code used to identify or reference the record in operational processes and integrations.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating QualityPlan records, where
        applicable.
      relationshipContext: Its meaning is specific to QualityPlan; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: name
    type: string
    required: true
    maxLength: 300
    help:
      summary: The human-readable name used by people, reports, searches, and related business processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating QualityPlan records, where
        applicable.
      relationshipContext: Its meaning is specific to QualityPlan; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - DRAFT
    - ACTIVE
    - SUSPENDED
    - RETIRED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating QualityPlan records, where
        applicable.
      relationshipContext: Its meaning is specific to QualityPlan; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        DRAFT: Represents the draft state or classification in the context of QualityPlan.
        ACTIVE: Represents the active state or classification in the context of QualityPlan.
        SUSPENDED: Represents the suspended state or classification in the context of QualityPlan.
        RETIRED: Represents the retired state or classification in the context of QualityPlan.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: product
    target: Product
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects QualityPlan to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a QualityPlan.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: inspections
    target: QualityInspection
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects QualityPlan to QualityInspection so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about QualityInspection records associated with a QualityPlan.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: QUALITY-PLAN-001
    rule: code must be unique within the owning ERP context.
  help:
    summary: Represents a quality definition called QualityPlan within the CEDM business model.
    businessMeaning: QualityPlan is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate QualityPlan records.
    relationshipContext: The entity participates in a wider business graph through relationships with Product, QualityInspection.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical QualityPlan record represents one identifiable business occurrence or master-data object that can be
      referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-QUALITY-PLAN

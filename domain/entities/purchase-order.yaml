entity:
  name: PurchaseOrder
  namespace: cedm.domain
  kind: procurement_transaction
  description: A request issued to a supplier for goods or services.
  identity:
    key: purchaseOrderId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: purchaseOrderId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the purchase order record associated with this PurchaseOrder. The identifier connects this record
        to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseOrder records, where
        applicable.
      relationshipContext: Its meaning is specific to PurchaseOrder; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: orderNumber
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of order number for the PurchaseOrder. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseOrder records, where
        applicable.
      relationshipContext: Its meaning is specific to PurchaseOrder; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: orderDate
    type: datetime
    required: true
    help:
      summary: Records the business date associated with the order. It is used in chronology, eligibility, scheduling, reporting,
        and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseOrder records, where
        applicable.
      relationshipContext: Its meaning is specific to PurchaseOrder; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: DRAFT
    values:
    - DRAFT
    - APPROVED
    - SENT
    - PARTIALLY_RECEIVED
    - RECEIVED
    - CANCELLED
    - CLOSED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseOrder records, where
        applicable.
      relationshipContext: Its meaning is specific to PurchaseOrder; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        DRAFT: Represents the draft state or classification in the context of PurchaseOrder.
        APPROVED: Represents the approved state or classification in the context of PurchaseOrder.
        SENT: Represents the sent state or classification in the context of PurchaseOrder.
        PARTIALLY_RECEIVED: Represents the partially received state or classification in the context of PurchaseOrder.
        RECEIVED: Represents the received state or classification in the context of PurchaseOrder.
        CANCELLED: Represents the cancelled state or classification in the context of PurchaseOrder.
        CLOSED: Represents the closed state or classification in the context of PurchaseOrder.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: currencyId
    type: reference
    target: Currency
    required: false
    help:
      summary: Identifies the currency in which monetary amounts on the record are expressed, allowing amounts to be interpreted
        and aggregated consistently.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseOrder records, where
        applicable.
      relationshipContext: This field connects PurchaseOrder to Currency. The reference establishes business context between
        the two entities and lets processes navigate from this record to the related Currency.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: requestedDeliveryDate
    type: date
    required: false
    help:
      summary: Records the business date associated with the requested delivery. It is used in chronology, eligibility, scheduling,
        reporting, and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseOrder records, where
        applicable.
      relationshipContext: Its meaning is specific to PurchaseOrder; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: totalAmount
    type: decimal
    required: false
    precision: 20
    scale: 4
    help:
      summary: Captures the business meaning of total amount for the PurchaseOrder. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseOrder records, where
        applicable.
      relationshipContext: Its meaning is specific to PurchaseOrder; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: supplier
    target: Supplier
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects PurchaseOrder to Supplier so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Supplier records associated with a PurchaseOrder.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: organization
    target: Organization
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects PurchaseOrder to Organization so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Organization records associated with a PurchaseOrder.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: lines
    target: PurchaseOrderLine
    cardinality: 1..*
    ownership: aggregate
    help:
      summary: Connects PurchaseOrder to PurchaseOrderLine so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about PurchaseOrderLine records associated with a PurchaseOrder.
      cardinalityMeaning: The declared cardinality 1..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: deliveryLocation
    target: Location
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects PurchaseOrder to Location so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Location records associated with a PurchaseOrder.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: PURCHASE-ORDER-001
    rule: A submitted purchase order must contain at least one line.
  help:
    summary: Represents a procurement transaction called PurchaseOrder within the CEDM business model.
    businessMeaning: PurchaseOrder is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate PurchaseOrder records.
    relationshipContext: The entity participates in a wider business graph through relationships with Supplier, Organization,
      PurchaseOrderLine, Location. These relationships provide the context needed to interpret the record rather than treating
      its fields as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical PurchaseOrder record represents one identifiable business occurrence or master-data object that can
      be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-PURCHASE-ORDER

entity:
  name: ShoppingCart
  namespace: cedm.domain
  kind: ecommerce_entity
  description: A temporary collection of products selected by a customer or shopper before checkout.
  identity:
    key: shoppingCartId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: shoppingCartId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the shopping cart record associated with this ShoppingCart. The identifier connects this record
        to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ShoppingCart records, where
        applicable.
      relationshipContext: Its meaning is specific to ShoppingCart; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - ACTIVE
    - CHECKED_OUT
    - ABANDONED
    - EXPIRED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ShoppingCart records, where
        applicable.
      relationshipContext: Its meaning is specific to ShoppingCart; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        ACTIVE: Represents the active state or classification in the context of ShoppingCart.
        CHECKED_OUT: Represents the checked out state or classification in the context of ShoppingCart.
        ABANDONED: Represents the abandoned state or classification in the context of ShoppingCart.
        EXPIRED: Represents the expired state or classification in the context of ShoppingCart.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: createdAt
    type: datetime
    required: true
    help:
      summary: The system-managed timestamp recording when the record was created. It supports auditability, chronology, and
        operational reporting.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ShoppingCart records, where
        applicable.
      relationshipContext: Its meaning is specific to ShoppingCart; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: expiresAt
    type: datetime
    required: false
    help:
      summary: Records when the expires event occurred. It establishes chronology, supports auditability, and helps coordinate
        related lifecycle and process activities.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ShoppingCart records, where
        applicable.
      relationshipContext: Its meaning is specific to ShoppingCart; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: customer
    target: Customer
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects ShoppingCart to Customer so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Customer records associated with a ShoppingCart.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: lines
    target: ShoppingCartLine
    cardinality: 0..*
    ownership: aggregate
    help:
      summary: Connects ShoppingCart to ShoppingCartLine so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about ShoppingCartLine records associated with a ShoppingCart.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: CART-001
    rule: a CHECKED_OUT cart cannot be modified.
  help:
    summary: Represents a ecommerce entity called ShoppingCart within the CEDM business model.
    businessMeaning: ShoppingCart is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate ShoppingCart records.
    relationshipContext: The entity participates in a wider business graph through relationships with Customer, ShoppingCartLine.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical ShoppingCart record represents one identifiable business occurrence or master-data object that can
      be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-SHOPPING-CART

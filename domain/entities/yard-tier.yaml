entity:
  name: YardTier
  namespace: cedm.domain
  kind: logistics_location
  description: A vertical tier level within a yard bay.
  identity:
    key: yardTierId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: yardTierId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the yard tier record associated with this YardTier. The identifier connects this record to the corresponding
        CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating YardTier records, where applicable.
      relationshipContext: Its meaning is specific to YardTier; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: tierNumber
    type: integer
    required: true
    minimum: 1
    help:
      summary: Captures the business meaning of tier number for the YardTier. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating YardTier records, where applicable.
      relationshipContext: Its meaning is specific to YardTier; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - ACTIVE
    - BLOCKED
    - CLOSED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating YardTier records, where applicable.
      relationshipContext: Its meaning is specific to YardTier; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        ACTIVE: Represents the active state or classification in the context of YardTier.
        BLOCKED: Represents the blocked state or classification in the context of YardTier.
        CLOSED: Represents the closed state or classification in the context of YardTier.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: bay
    target: YardBay
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects YardTier to YardBay so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about YardBay records associated with a YardTier.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: slots
    target: YardSlot
    cardinality: 1..*
    ownership: aggregate
    help:
      summary: Connects YardTier to YardSlot so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about YardSlot records associated with a YardTier.
      cardinalityMeaning: The declared cardinality 1..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: YARD-TIER-001
    rule: tierNumber must be unique within a bay.
  help:
    summary: Represents a logistics location called YardTier within the CEDM business model.
    businessMeaning: YardTier is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate YardTier records.
    relationshipContext: The entity participates in a wider business graph through relationships with YardBay, YardSlot. These
      relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical YardTier record represents one identifiable business occurrence or master-data object that can be referenced
      by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-YARD-TIER

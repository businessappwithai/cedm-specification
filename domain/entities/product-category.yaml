entity:
  name: ProductCategory
  namespace: cedm.domain
  kind: classification_entity
  description: A hierarchical classification used to organize products and services.
  identity:
    key: productCategoryId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: productCategoryId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the product category record associated with this ProductCategory. The identifier connects this record
        to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ProductCategory records, where
        applicable.
      relationshipContext: Its meaning is specific to ProductCategory; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: code
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: A human-readable business code used to identify or reference the record in operational processes and integrations.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ProductCategory records, where
        applicable.
      relationshipContext: Its meaning is specific to ProductCategory; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: name
    type: string
    required: true
    maxLength: 200
    help:
      summary: The human-readable name used by people, reports, searches, and related business processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ProductCategory records, where
        applicable.
      relationshipContext: Its meaning is specific to ProductCategory; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: description
    type: string
    required: false
    maxLength: 2000
    help:
      summary: A business description that explains the purpose, scope, or meaning of the record to users and downstream processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ProductCategory records, where
        applicable.
      relationshipContext: Its meaning is specific to ProductCategory; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - ACTIVE
    - INACTIVE
    - RETIRED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ProductCategory records, where
        applicable.
      relationshipContext: Its meaning is specific to ProductCategory; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        ACTIVE: Represents the active state or classification in the context of ProductCategory.
        INACTIVE: Represents the inactive state or classification in the context of ProductCategory.
        RETIRED: Represents the retired state or classification in the context of ProductCategory.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: parentCategory
    target: ProductCategory
    cardinality: 0..1
    ownership: reference
    inverse: childCategories
    help:
      summary: Connects ProductCategory to ProductCategory so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about ProductCategory records associated with a ProductCategory.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: childCategories
    target: ProductCategory
    cardinality: 0..*
    ownership: reference
    inverse: parentCategory
    help:
      summary: Connects ProductCategory to ProductCategory so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about ProductCategory records associated with a ProductCategory.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: products
    target: Product
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects ProductCategory to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a ProductCategory.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: PRODUCT-CATEGORY-001
    rule: A category cannot be its own parent.
  - id: PRODUCT-CATEGORY-002
    rule: The category hierarchy must not contain cycles.
  help:
    summary: Represents a classification entity called ProductCategory within the CEDM business model.
    businessMeaning: ProductCategory is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate ProductCategory records.
    relationshipContext: The entity participates in a wider business graph through relationships with ProductCategory, ProductCategory,
      Product. These relationships provide the context needed to interpret the record rather than treating its fields as isolated
      database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical ProductCategory record represents one identifiable business occurrence or master-data object that can
      be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-PRODUCT-CATEGORY

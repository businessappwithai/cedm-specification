entity:
  name: SecurityIncident
  namespace: cedm.domain
  kind: security_entity
  description: A recorded event or condition that may compromise confidentiality, integrity, availability, safety, or security.
  identity:
    key: securityIncidentId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: securityIncidentId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the security incident record associated with this SecurityIncident. The identifier connects this
        record to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SecurityIncident records,
        where applicable.
      relationshipContext: Its meaning is specific to SecurityIncident; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: incidentNumber
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of incident number for the SecurityIncident. It is interpreted together with
        the entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SecurityIncident records,
        where applicable.
      relationshipContext: Its meaning is specific to SecurityIncident; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: incidentType
    type: string
    required: true
    maxLength: 100
    help:
      summary: Captures the business meaning of incident type for the SecurityIncident. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SecurityIncident records,
        where applicable.
      relationshipContext: Its meaning is specific to SecurityIncident; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: severity
    type: enum
    required: true
    values:
    - LOW
    - MEDIUM
    - HIGH
    - CRITICAL
    help:
      summary: Captures the business meaning of severity for the SecurityIncident. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SecurityIncident records,
        where applicable.
      relationshipContext: Its meaning is specific to SecurityIncident; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        LOW: Represents the low state or classification in the context of SecurityIncident.
        MEDIUM: Represents the medium state or classification in the context of SecurityIncident.
        HIGH: Represents the high state or classification in the context of SecurityIncident.
        CRITICAL: Represents the critical state or classification in the context of SecurityIncident.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: OPEN
    values:
    - OPEN
    - INVESTIGATING
    - CONTAINED
    - RESOLVED
    - CLOSED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SecurityIncident records,
        where applicable.
      relationshipContext: Its meaning is specific to SecurityIncident; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        OPEN: Represents the open state or classification in the context of SecurityIncident.
        INVESTIGATING: Represents the investigating state or classification in the context of SecurityIncident.
        CONTAINED: Represents the contained state or classification in the context of SecurityIncident.
        RESOLVED: Represents the resolved state or classification in the context of SecurityIncident.
        CLOSED: Represents the closed state or classification in the context of SecurityIncident.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: detectedAt
    type: datetime
    required: true
    help:
      summary: Records when the detected event occurred. It establishes chronology, supports auditability, and helps coordinate
        related lifecycle and process activities.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SecurityIncident records,
        where applicable.
      relationshipContext: Its meaning is specific to SecurityIncident; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: resolvedAt
    type: datetime
    required: false
    help:
      summary: Records when the resolved event occurred. It establishes chronology, supports auditability, and helps coordinate
        related lifecycle and process activities.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SecurityIncident records,
        where applicable.
      relationshipContext: Its meaning is specific to SecurityIncident; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: organization
    target: Organization
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects SecurityIncident to Organization so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Organization records associated with a SecurityIncident.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: reporter
    target: Party
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects SecurityIncident to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a SecurityIncident.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: investigator
    target: Party
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects SecurityIncident to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a SecurityIncident.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: relatedRisk
    target: Risk
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects SecurityIncident to Risk so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Risk records associated with a SecurityIncident.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: SECURITY-INCIDENT-001
    rule: resolvedAt must not precede detectedAt.
  help:
    summary: Represents a security entity called SecurityIncident within the CEDM business model.
    businessMeaning: SecurityIncident is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate SecurityIncident records.
    relationshipContext: The entity participates in a wider business graph through relationships with Organization, Party,
      Party, Risk. These relationships provide the context needed to interpret the record rather than treating its fields
      as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical SecurityIncident record represents one identifiable business occurrence or master-data object that
      can be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-SECURITY-INCIDENT

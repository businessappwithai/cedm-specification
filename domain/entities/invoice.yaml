entity:
  name: Invoice
  namespace: cedm.domain
  kind: financial_commercial_transaction
  description: A commercial document requesting or recording payment for supplied goods or services.
  identity:
    key: invoiceId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: invoiceId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the invoice record associated with this Invoice. The identifier connects this record to the corresponding
        CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: Its meaning is specific to Invoice; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: invoiceNumber
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of invoice number for the Invoice. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: Its meaning is specific to Invoice; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: invoiceDate
    type: date
    required: true
    help:
      summary: Records the business date associated with the invoice. It is used in chronology, eligibility, scheduling, reporting,
        and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: Its meaning is specific to Invoice; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: dueDate
    type: date
    required: false
    help:
      summary: Records the business date associated with the due. It is used in chronology, eligibility, scheduling, reporting,
        and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: Its meaning is specific to Invoice; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: invoiceType
    type: enum
    required: true
    values:
    - SALES
    - PURCHASE
    - CREDIT_NOTE
    - DEBIT_NOTE
    help:
      summary: Captures the business meaning of invoice type for the Invoice. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: Its meaning is specific to Invoice; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        SALES: Represents the sales state or classification in the context of Invoice.
        PURCHASE: Represents the purchase state or classification in the context of Invoice.
        CREDIT_NOTE: Represents the credit note state or classification in the context of Invoice.
        DEBIT_NOTE: Represents the debit note state or classification in the context of Invoice.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: DRAFT
    values:
    - DRAFT
    - ISSUED
    - PARTIALLY_PAID
    - PAID
    - OVERDUE
    - CANCELLED
    - VOID
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: Its meaning is specific to Invoice; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        DRAFT: Represents the draft state or classification in the context of Invoice.
        ISSUED: Represents the issued state or classification in the context of Invoice.
        PARTIALLY_PAID: Represents the partially paid state or classification in the context of Invoice.
        PAID: Represents the paid state or classification in the context of Invoice.
        OVERDUE: Represents the overdue state or classification in the context of Invoice.
        CANCELLED: Represents the cancelled state or classification in the context of Invoice.
        VOID: Represents the void state or classification in the context of Invoice.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: currencyId
    type: reference
    target: Currency
    required: true
    help:
      summary: Identifies the currency in which monetary amounts on the record are expressed, allowing amounts to be interpreted
        and aggregated consistently.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: This field connects Invoice to Currency. The reference establishes business context between the
        two entities and lets processes navigate from this record to the related Currency.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: subtotal
    type: decimal
    required: true
    precision: 20
    scale: 4
    help:
      summary: Captures the business meaning of subtotal for the Invoice. It is interpreted together with the entity's other
        attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: Its meaning is specific to Invoice; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: taxAmount
    type: decimal
    required: true
    precision: 20
    scale: 4
    help:
      summary: Captures the business meaning of tax amount for the Invoice. It is interpreted together with the entity's other
        attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: Its meaning is specific to Invoice; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: totalAmount
    type: decimal
    required: true
    precision: 20
    scale: 4
    help:
      summary: Captures the business meaning of total amount for the Invoice. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Invoice records, where applicable.
      relationshipContext: Its meaning is specific to Invoice; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: customer
    target: Customer
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Invoice to Customer so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Customer records associated with a Invoice.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: supplier
    target: Supplier
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Invoice to Supplier so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Supplier records associated with a Invoice.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: lines
    target: InvoiceLine
    cardinality: 1..*
    ownership: aggregate
    help:
      summary: Connects Invoice to InvoiceLine so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about InvoiceLine records associated with a Invoice.
      cardinalityMeaning: The declared cardinality 1..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: paymentTerms
    target: PaymentTerm
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Invoice to PaymentTerm so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about PaymentTerm records associated with a Invoice.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: salesOrder
    target: SalesOrder
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Invoice to SalesOrder so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about SalesOrder records associated with a Invoice.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: purchaseOrder
    target: PurchaseOrder
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Invoice to PurchaseOrder so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about PurchaseOrder records associated with a Invoice.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: INVOICE-001
    rule: totalAmount equals subtotal plus tax and applicable adjustments according to invoice policy.
  - id: INVOICE-002
    rule: dueDate must not precede invoiceDate.
  help:
    summary: Represents a financial commercial transaction called Invoice within the CEDM business model.
    businessMeaning: Invoice is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate Invoice records.
    relationshipContext: The entity participates in a wider business graph through relationships with Customer, Supplier,
      InvoiceLine, PaymentTerm, SalesOrder, PurchaseOrder. These relationships provide the context needed to interpret the
      record rather than treating its fields as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical Invoice record represents one identifiable business occurrence or master-data object that can be referenced
      by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-INVOICE

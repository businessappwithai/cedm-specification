entity:
  name: Position
  namespace: cedm.domain
  kind: workforce_master_entity
  description: A defined organizational role with responsibilities, reporting structure, and staffing requirements.
  identity:
    key: positionId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: positionId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the position record associated with this Position. The identifier connects this record to the corresponding
        CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Position records, where applicable.
      relationshipContext: Its meaning is specific to Position; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: code
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: A human-readable business code used to identify or reference the record in operational processes and integrations.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Position records, where applicable.
      relationshipContext: Its meaning is specific to Position; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: title
    type: string
    required: true
    maxLength: 200
    help:
      summary: Captures the business meaning of title for the Position. It is interpreted together with the entity's other
        attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Position records, where applicable.
      relationshipContext: Its meaning is specific to Position; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: OPEN
    values:
    - OPEN
    - FILLED
    - FROZEN
    - CLOSED
    - RETIRED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Position records, where applicable.
      relationshipContext: Its meaning is specific to Position; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        OPEN: Represents the open state or classification in the context of Position.
        FILLED: Represents the filled state or classification in the context of Position.
        FROZEN: Represents the frozen state or classification in the context of Position.
        CLOSED: Represents the closed state or classification in the context of Position.
        RETIRED: Represents the retired state or classification in the context of Position.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: effectiveFrom
    type: date
    required: false
    help:
      summary: Captures the business meaning of effective from for the Position. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Position records, where applicable.
      relationshipContext: Its meaning is specific to Position; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: effectiveTo
    type: date
    required: false
    help:
      summary: Captures the business meaning of effective to for the Position. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Position records, where applicable.
      relationshipContext: Its meaning is specific to Position; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: organization
    target: Organization
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects Position to Organization so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Organization records associated with a Position.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: parentPosition
    target: Position
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Position to Position so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Position records associated with a Position.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: employees
    target: Employee
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects Position to Employee so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Employee records associated with a Position.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: POSITION-001
    rule: effectiveTo must not precede effectiveFrom.
  help:
    summary: Represents a workforce master entity called Position within the CEDM business model.
    businessMeaning: Position is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate Position records.
    relationshipContext: The entity participates in a wider business graph through relationships with Organization, Position,
      Employee. These relationships provide the context needed to interpret the record rather than treating its fields as
      isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical Position record represents one identifiable business occurrence or master-data object that can be referenced
      by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-POSITION

entity:
  name: CorrectiveAction
  namespace: cedm.domain
  kind: quality_process_entity
  description: An action intended to eliminate the cause of a detected nonconformance or prevent recurrence.
  identity:
    key: correctiveActionId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: correctiveActionId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the corrective action record associated with this CorrectiveAction. The identifier connects this
        record to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating CorrectiveAction records,
        where applicable.
      relationshipContext: Its meaning is specific to CorrectiveAction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: actionNumber
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of action number for the CorrectiveAction. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating CorrectiveAction records,
        where applicable.
      relationshipContext: Its meaning is specific to CorrectiveAction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: actionType
    type: enum
    required: true
    values:
    - CONTAINMENT
    - CORRECTION
    - CORRECTIVE
    - PREVENTIVE
    help:
      summary: Captures the business meaning of action type for the CorrectiveAction. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating CorrectiveAction records,
        where applicable.
      relationshipContext: Its meaning is specific to CorrectiveAction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        CONTAINMENT: Represents the containment state or classification in the context of CorrectiveAction.
        CORRECTION: Represents the correction state or classification in the context of CorrectiveAction.
        CORRECTIVE: Represents the corrective state or classification in the context of CorrectiveAction.
        PREVENTIVE: Represents the preventive state or classification in the context of CorrectiveAction.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: description
    type: string
    required: true
    maxLength: 4000
    help:
      summary: A business description that explains the purpose, scope, or meaning of the record to users and downstream processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating CorrectiveAction records,
        where applicable.
      relationshipContext: Its meaning is specific to CorrectiveAction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: OPEN
    values:
    - OPEN
    - ASSIGNED
    - IN_PROGRESS
    - VERIFICATION
    - COMPLETED
    - CANCELLED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating CorrectiveAction records,
        where applicable.
      relationshipContext: Its meaning is specific to CorrectiveAction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        OPEN: Represents the open state or classification in the context of CorrectiveAction.
        ASSIGNED: Represents the assigned state or classification in the context of CorrectiveAction.
        IN_PROGRESS: Represents the in progress state or classification in the context of CorrectiveAction.
        VERIFICATION: Represents the verification state or classification in the context of CorrectiveAction.
        COMPLETED: Represents the completed state or classification in the context of CorrectiveAction.
        CANCELLED: Represents the cancelled state or classification in the context of CorrectiveAction.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: dueDate
    type: date
    required: false
    help:
      summary: Records the business date associated with the due. It is used in chronology, eligibility, scheduling, reporting,
        and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating CorrectiveAction records,
        where applicable.
      relationshipContext: Its meaning is specific to CorrectiveAction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: completedDate
    type: date
    required: false
    help:
      summary: Records the business date associated with the completed. It is used in chronology, eligibility, scheduling,
        reporting, and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating CorrectiveAction records,
        where applicable.
      relationshipContext: Its meaning is specific to CorrectiveAction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: nonconformance
    target: Nonconformance
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects CorrectiveAction to Nonconformance so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Nonconformance records associated with a CorrectiveAction.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: owner
    target: Party
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects CorrectiveAction to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a CorrectiveAction.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: CORRECTIVE-ACTION-001
    rule: completedDate must be present when status is COMPLETED.
  help:
    summary: Represents a quality process entity called CorrectiveAction within the CEDM business model.
    businessMeaning: CorrectiveAction is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate CorrectiveAction records.
    relationshipContext: The entity participates in a wider business graph through relationships with Nonconformance, Party.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical CorrectiveAction record represents one identifiable business occurrence or master-data object that
      can be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-CORRECTIVE-ACTION

entity:
  name: ProductionRecord
  namespace: cedm.domain
  kind: industrial_transaction
  description: A recorded quantity of output produced or extracted by an operational process.
  identity:
    key: productionRecordId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: productionRecordId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the production record record associated with this ProductionRecord. The identifier connects this
        record to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ProductionRecord records,
        where applicable.
      relationshipContext: Its meaning is specific to ProductionRecord; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: productionDate
    type: datetime
    required: true
    help:
      summary: Records the business date associated with the production. It is used in chronology, eligibility, scheduling,
        reporting, and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ProductionRecord records,
        where applicable.
      relationshipContext: Its meaning is specific to ProductionRecord; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: quantity
    type: decimal
    required: true
    precision: 20
    scale: 6
    help:
      summary: The amount of the referenced item expressed in the applicable unit of measure. It is used by calculations,
        inventory, planning, fulfillment, or other quantity-based processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ProductionRecord records,
        where applicable.
      relationshipContext: Its meaning is specific to ProductionRecord; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: RECORDED
    values:
    - PLANNED
    - RECORDED
    - VERIFIED
    - REJECTED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ProductionRecord records,
        where applicable.
      relationshipContext: Its meaning is specific to ProductionRecord; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        PLANNED: Represents the planned state or classification in the context of ProductionRecord.
        RECORDED: Represents the recorded state or classification in the context of ProductionRecord.
        VERIFIED: Represents the verified state or classification in the context of ProductionRecord.
        REJECTED: Represents the rejected state or classification in the context of ProductionRecord.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: product
    target: Product
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects ProductionRecord to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a ProductionRecord.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: location
    target: Location
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects ProductionRecord to Location so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Location records associated with a ProductionRecord.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: miningSite
    target: MiningSite
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects ProductionRecord to MiningSite so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about MiningSite records associated with a ProductionRecord.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: workOrder
    target: ManufacturingWorkOrder
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects ProductionRecord to ManufacturingWorkOrder so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about ManufacturingWorkOrder records associated with a ProductionRecord.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: PRODUCTION-001
    rule: quantity must be greater than zero.
  help:
    summary: Represents a industrial transaction called ProductionRecord within the CEDM business model.
    businessMeaning: ProductionRecord is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate ProductionRecord records.
    relationshipContext: The entity participates in a wider business graph through relationships with Product, Location, MiningSite,
      ManufacturingWorkOrder. These relationships provide the context needed to interpret the record rather than treating
      its fields as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical ProductionRecord record represents one identifiable business occurrence or master-data object that
      can be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-PRODUCTION-RECORD

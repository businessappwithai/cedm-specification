entity:
  name: ShoppingCartLine
  namespace: cedm.domain
  kind: ecommerce_detail_entity
  description: A product selection and quantity within a shopping cart.
  identity:
    key: shoppingCartLineId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: shoppingCartLineId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the shopping cart line record associated with this ShoppingCartLine. The identifier connects this
        record to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ShoppingCartLine records,
        where applicable.
      relationshipContext: Its meaning is specific to ShoppingCartLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: quantity
    type: decimal
    required: true
    precision: 18
    scale: 6
    help:
      summary: The amount of the referenced item expressed in the applicable unit of measure. It is used by calculations,
        inventory, planning, fulfillment, or other quantity-based processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ShoppingCartLine records,
        where applicable.
      relationshipContext: Its meaning is specific to ShoppingCartLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: unitPrice
    type: money
    required: false
    help:
      summary: Captures the business meaning of unit price for the ShoppingCartLine. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ShoppingCartLine records,
        where applicable.
      relationshipContext: Its meaning is specific to ShoppingCartLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: cart
    target: ShoppingCart
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects ShoppingCartLine to ShoppingCart so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about ShoppingCart records associated with a ShoppingCartLine.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: product
    target: Product
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects ShoppingCartLine to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a ShoppingCartLine.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: CART-LINE-001
    rule: quantity must be greater than zero.
  help:
    summary: Represents a ecommerce detail entity called ShoppingCartLine within the CEDM business model.
    businessMeaning: ShoppingCartLine is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate ShoppingCartLine records.
    relationshipContext: The entity participates in a wider business graph through relationships with ShoppingCart, Product.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical ShoppingCartLine record represents one identifiable business occurrence or master-data object that
      can be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-SHOPPING-CART-LINE

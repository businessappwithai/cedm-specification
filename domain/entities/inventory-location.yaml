entity:
  name: InventoryLocation
  namespace: cedm.domain
  kind: inventory_master_entity
  description: A specific logical storage position within a warehouse or managed location.
  identity:
    key: inventoryLocationId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: inventoryLocationId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the inventory location record associated with this InventoryLocation. The identifier connects this
        record to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryLocation records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryLocation; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: code
    type: string
    required: true
    maxLength: 100
    help:
      summary: A human-readable business code used to identify or reference the record in operational processes and integrations.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryLocation records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryLocation; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: locationType
    type: enum
    required: true
    values:
    - BIN
    - SHELF
    - RACK
    - FLOOR
    - YARD_SLOT
    - OTHER
    help:
      summary: Captures the business meaning of location type for the InventoryLocation. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryLocation records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryLocation; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        BIN: Represents the bin state or classification in the context of InventoryLocation.
        SHELF: Represents the shelf state or classification in the context of InventoryLocation.
        RACK: Represents the rack state or classification in the context of InventoryLocation.
        FLOOR: Represents the floor state or classification in the context of InventoryLocation.
        YARD_SLOT: Represents the yard slot state or classification in the context of InventoryLocation.
        OTHER: Represents the other state or classification in the context of InventoryLocation.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - ACTIVE
    - BLOCKED
    - INACTIVE
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryLocation records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryLocation; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        ACTIVE: Represents the active state or classification in the context of InventoryLocation.
        BLOCKED: Represents the blocked state or classification in the context of InventoryLocation.
        INACTIVE: Represents the inactive state or classification in the context of InventoryLocation.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: capacity
    type: decimal
    required: false
    precision: 20
    scale: 4
    help:
      summary: Captures the business meaning of capacity for the InventoryLocation. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryLocation records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryLocation; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: warehouse
    target: Warehouse
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects InventoryLocation to Warehouse so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Warehouse records associated with a InventoryLocation.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: inventoryBalances
    target: InventoryBalance
    cardinality: 0..*
    ownership: aggregate
    help:
      summary: Connects InventoryLocation to InventoryBalance so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about InventoryBalance records associated with a InventoryLocation.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: INVENTORY-LOCATION-001
    rule: code must be unique within its warehouse.
  help:
    summary: Represents a inventory master entity called InventoryLocation within the CEDM business model.
    businessMeaning: InventoryLocation is a business concept with its own identity and lifecycle. It captures information
      that must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate InventoryLocation records.
    relationshipContext: The entity participates in a wider business graph through relationships with Warehouse, InventoryBalance.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical InventoryLocation record represents one identifiable business occurrence or master-data object that
      can be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-INVENTORY-LOCATION

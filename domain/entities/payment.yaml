entity:
  name: Payment
  namespace: cedm.domain
  kind: financial_transaction
  description: A receipt or disbursement that settles, partially settles, or advances a financial obligation.
  identity:
    key: paymentId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: paymentId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the payment record associated with this Payment. The identifier connects this record to the corresponding
        CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Payment records, where applicable.
      relationshipContext: Its meaning is specific to Payment; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: paymentNumber
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of payment number for the Payment. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Payment records, where applicable.
      relationshipContext: Its meaning is specific to Payment; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: paymentDate
    type: datetime
    required: true
    help:
      summary: Records the business date associated with the payment. It is used in chronology, eligibility, scheduling, reporting,
        and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Payment records, where applicable.
      relationshipContext: Its meaning is specific to Payment; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: direction
    type: enum
    required: true
    values:
    - RECEIPT
    - DISBURSEMENT
    help:
      summary: Captures the business meaning of direction for the Payment. It is interpreted together with the entity's other
        attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Payment records, where applicable.
      relationshipContext: Its meaning is specific to Payment; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        RECEIPT: Represents the receipt state or classification in the context of Payment.
        DISBURSEMENT: Represents the disbursement state or classification in the context of Payment.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: DRAFT
    values:
    - DRAFT
    - APPROVED
    - POSTED
    - CLEARED
    - VOID
    - REVERSED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Payment records, where applicable.
      relationshipContext: Its meaning is specific to Payment; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        DRAFT: Represents the draft state or classification in the context of Payment.
        APPROVED: Represents the approved state or classification in the context of Payment.
        POSTED: Represents the posted state or classification in the context of Payment.
        CLEARED: Represents the cleared state or classification in the context of Payment.
        VOID: Represents the void state or classification in the context of Payment.
        REVERSED: Represents the reversed state or classification in the context of Payment.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: amount
    type: decimal
    required: true
    precision: 20
    scale: 4
    help:
      summary: A monetary amount representing the financial value of the record or transaction. Its interpretation depends
        on the surrounding business context and currency.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Payment records, where applicable.
      relationshipContext: Its meaning is specific to Payment; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: currencyId
    type: reference
    target: Currency
    required: true
    help:
      summary: Identifies the currency in which monetary amounts on the record are expressed, allowing amounts to be interpreted
        and aggregated consistently.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Payment records, where applicable.
      relationshipContext: This field connects Payment to Currency. The reference establishes business context between the
        two entities and lets processes navigate from this record to the related Currency.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: paymentMethod
    type: enum
    required: true
    values:
    - CASH
    - BANK_TRANSFER
    - CARD
    - CHEQUE
    - DIRECT_DEBIT
    - OTHER
    help:
      summary: Captures the business meaning of payment method for the Payment. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Payment records, where applicable.
      relationshipContext: Its meaning is specific to Payment; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        CASH: Represents the cash state or classification in the context of Payment.
        BANK_TRANSFER: Represents the bank transfer state or classification in the context of Payment.
        CARD: Represents the card state or classification in the context of Payment.
        CHEQUE: Represents the cheque state or classification in the context of Payment.
        DIRECT_DEBIT: Represents the direct debit state or classification in the context of Payment.
        OTHER: Represents the other state or classification in the context of Payment.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: payer
    target: Party
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Payment to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a Payment.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: payee
    target: Party
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Payment to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a Payment.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: invoices
    target: Invoice
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects Payment to Invoice so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Invoice records associated with a Payment.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: PAYMENT-001
    rule: amount must be greater than zero.
  - id: PAYMENT-002
    rule: a REVERSED payment must reference an original payment or reversal relationship.
  help:
    summary: Represents a financial transaction called Payment within the CEDM business model.
    businessMeaning: Payment is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate Payment records.
    relationshipContext: The entity participates in a wider business graph through relationships with Party, Party, Invoice.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical Payment record represents one identifiable business occurrence or master-data object that can be referenced
      by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-PAYMENT

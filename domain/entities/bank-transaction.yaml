entity:
  name: BankTransaction
  namespace: cedm.domain
  kind: financial_transaction
  description: A monetary transaction recorded against a bank account.
  identity:
    key: bankTransactionId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: bankTransactionId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the bank transaction record associated with this BankTransaction. The identifier connects this record
        to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankTransaction records, where
        applicable.
      relationshipContext: Its meaning is specific to BankTransaction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: transactionNumber
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of transaction number for the BankTransaction. It is interpreted together with
        the entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankTransaction records, where
        applicable.
      relationshipContext: Its meaning is specific to BankTransaction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: transactionDate
    type: datetime
    required: true
    help:
      summary: Records the business date associated with the transaction. It is used in chronology, eligibility, scheduling,
        reporting, and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankTransaction records, where
        applicable.
      relationshipContext: Its meaning is specific to BankTransaction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: transactionType
    type: enum
    required: true
    values:
    - CREDIT
    - DEBIT
    - TRANSFER
    - FEE
    - INTEREST
    - REVERSAL
    - OTHER
    help:
      summary: Captures the business meaning of transaction type for the BankTransaction. It is interpreted together with
        the entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankTransaction records, where
        applicable.
      relationshipContext: Its meaning is specific to BankTransaction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        CREDIT: Represents the credit state or classification in the context of BankTransaction.
        DEBIT: Represents the debit state or classification in the context of BankTransaction.
        TRANSFER: Represents the transfer state or classification in the context of BankTransaction.
        FEE: Represents the fee state or classification in the context of BankTransaction.
        INTEREST: Represents the interest state or classification in the context of BankTransaction.
        REVERSAL: Represents the reversal state or classification in the context of BankTransaction.
        OTHER: Represents the other state or classification in the context of BankTransaction.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: amount
    type: decimal
    required: true
    precision: 20
    scale: 4
    help:
      summary: A monetary amount representing the financial value of the record or transaction. Its interpretation depends
        on the surrounding business context and currency.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankTransaction records, where
        applicable.
      relationshipContext: Its meaning is specific to BankTransaction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: POSTED
    values:
    - PENDING
    - POSTED
    - REVERSED
    - FAILED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankTransaction records, where
        applicable.
      relationshipContext: Its meaning is specific to BankTransaction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        PENDING: Represents the pending state or classification in the context of BankTransaction.
        POSTED: Represents the posted state or classification in the context of BankTransaction.
        REVERSED: Represents the reversed state or classification in the context of BankTransaction.
        FAILED: Represents the failed state or classification in the context of BankTransaction.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: reference
    type: string
    required: false
    maxLength: 200
    help:
      summary: Captures the business meaning of reference for the BankTransaction. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankTransaction records, where
        applicable.
      relationshipContext: Its meaning is specific to BankTransaction; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: account
    target: BankAccount
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects BankTransaction to BankAccount so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about BankAccount records associated with a BankTransaction.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: counterparty
    target: Party
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects BankTransaction to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a BankTransaction.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: BANK-TRANSACTION-001
    rule: amount must be greater than or equal to zero and transactionType determines debit or credit semantics.
  help:
    summary: Represents a financial transaction called BankTransaction within the CEDM business model.
    businessMeaning: BankTransaction is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate BankTransaction records.
    relationshipContext: The entity participates in a wider business graph through relationships with BankAccount, Party.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical BankTransaction record represents one identifiable business occurrence or master-data object that can
      be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-BANK-TRANSACTION

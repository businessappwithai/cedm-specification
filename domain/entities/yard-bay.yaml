entity:
  name: YardBay
  namespace: cedm.domain
  kind: logistics_location
  description: A bay within a yard block representing a longitudinal container position.
  identity:
    key: yardBayId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: yardBayId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the yard bay record associated with this YardBay. The identifier connects this record to the corresponding
        CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating YardBay records, where applicable.
      relationshipContext: Its meaning is specific to YardBay; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: code
    type: string
    required: true
    maxLength: 50
    help:
      summary: A human-readable business code used to identify or reference the record in operational processes and integrations.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating YardBay records, where applicable.
      relationshipContext: Its meaning is specific to YardBay; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: bayNumber
    type: integer
    required: true
    minimum: 1
    help:
      summary: Captures the business meaning of bay number for the YardBay. It is interpreted together with the entity's other
        attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating YardBay records, where applicable.
      relationshipContext: Its meaning is specific to YardBay; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - ACTIVE
    - BLOCKED
    - CLOSED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating YardBay records, where applicable.
      relationshipContext: Its meaning is specific to YardBay; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        ACTIVE: Represents the active state or classification in the context of YardBay.
        BLOCKED: Represents the blocked state or classification in the context of YardBay.
        CLOSED: Represents the closed state or classification in the context of YardBay.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: block
    target: YardBlock
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects YardBay to YardBlock so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about YardBlock records associated with a YardBay.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: tiers
    target: YardTier
    cardinality: 1..*
    ownership: aggregate
    help:
      summary: Connects YardBay to YardTier so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about YardTier records associated with a YardBay.
      cardinalityMeaning: The declared cardinality 1..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: YARD-BAY-001
    rule: bayNumber must be unique within a block.
  help:
    summary: Represents a logistics location called YardBay within the CEDM business model.
    businessMeaning: YardBay is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate YardBay records.
    relationshipContext: The entity participates in a wider business graph through relationships with YardBlock, YardTier.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical YardBay record represents one identifiable business occurrence or master-data object that can be referenced
      by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-YARD-BAY

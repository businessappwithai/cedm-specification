entity:
  name: SupplyPlanLine
  namespace: cedm.domain
  kind: supply_chain_entity
  description: A planned quantity for a product at a location and time within a supply plan.
  identity:
    key: supplyPlanLineId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: supplyPlanLineId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the supply plan line record associated with this SupplyPlanLine. The identifier connects this record
        to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SupplyPlanLine records, where
        applicable.
      relationshipContext: Its meaning is specific to SupplyPlanLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: planningDate
    type: date
    required: true
    help:
      summary: Records the business date associated with the planning. It is used in chronology, eligibility, scheduling,
        reporting, and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SupplyPlanLine records, where
        applicable.
      relationshipContext: Its meaning is specific to SupplyPlanLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: plannedQuantity
    type: decimal
    required: true
    precision: 20
    scale: 6
    help:
      summary: Captures the business meaning of planned quantity for the SupplyPlanLine. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SupplyPlanLine records, where
        applicable.
      relationshipContext: Its meaning is specific to SupplyPlanLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: safetyStockQuantity
    type: decimal
    required: false
    precision: 20
    scale: 6
    help:
      summary: Captures the business meaning of safety stock quantity for the SupplyPlanLine. It is interpreted together with
        the entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SupplyPlanLine records, where
        applicable.
      relationshipContext: Its meaning is specific to SupplyPlanLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: status
    type: enum
    required: true
    default: PLANNED
    values:
    - PLANNED
    - RELEASED
    - FULFILLED
    - CANCELLED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating SupplyPlanLine records, where
        applicable.
      relationshipContext: Its meaning is specific to SupplyPlanLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        PLANNED: Represents the planned state or classification in the context of SupplyPlanLine.
        RELEASED: Represents the released state or classification in the context of SupplyPlanLine.
        FULFILLED: Represents the fulfilled state or classification in the context of SupplyPlanLine.
        CANCELLED: Represents the cancelled state or classification in the context of SupplyPlanLine.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: supplyPlan
    target: SupplyPlan
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects SupplyPlanLine to SupplyPlan so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about SupplyPlan records associated with a SupplyPlanLine.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: product
    target: Product
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects SupplyPlanLine to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a SupplyPlanLine.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: location
    target: Location
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects SupplyPlanLine to Location so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Location records associated with a SupplyPlanLine.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: SUPPLY-PLAN-LINE-001
    rule: plannedQuantity must not be negative.
  help:
    summary: Represents a supply chain entity called SupplyPlanLine within the CEDM business model.
    businessMeaning: SupplyPlanLine is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate SupplyPlanLine records.
    relationshipContext: The entity participates in a wider business graph through relationships with SupplyPlan, Product,
      Location. These relationships provide the context needed to interpret the record rather than treating its fields as
      isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical SupplyPlanLine record represents one identifiable business occurrence or master-data object that can
      be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-SUPPLY-PLAN-LINE

entity:
  name: BankAccount
  namespace: cedm.domain
  kind: financial_entity
  description: A financial account maintained with a banking institution for a party or organization.
  identity:
    key: bankAccountId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: bankAccountId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the bank account record associated with this BankAccount. The identifier connects this record to
        the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankAccount records, where
        applicable.
      relationshipContext: Its meaning is specific to BankAccount; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: accountNumber
    type: string
    required: true
    maxLength: 100
    help:
      summary: Captures the business meaning of account number for the BankAccount. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankAccount records, where
        applicable.
      relationshipContext: Its meaning is specific to BankAccount; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: accountType
    type: enum
    required: true
    values:
    - CURRENT
    - SAVINGS
    - LOAN
    - ESCROW
    - OTHER
    help:
      summary: Captures the business meaning of account type for the BankAccount. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankAccount records, where
        applicable.
      relationshipContext: Its meaning is specific to BankAccount; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        CURRENT: Represents the current state or classification in the context of BankAccount.
        SAVINGS: Represents the savings state or classification in the context of BankAccount.
        LOAN: Represents the loan state or classification in the context of BankAccount.
        ESCROW: Represents the escrow state or classification in the context of BankAccount.
        OTHER: Represents the other state or classification in the context of BankAccount.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: currencyId
    type: reference
    target: Currency
    required: true
    help:
      summary: Identifies the currency in which monetary amounts on the record are expressed, allowing amounts to be interpreted
        and aggregated consistently.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankAccount records, where
        applicable.
      relationshipContext: This field connects BankAccount to Currency. The reference establishes business context between
        the two entities and lets processes navigate from this record to the related Currency.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - PENDING
    - ACTIVE
    - BLOCKED
    - CLOSED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating BankAccount records, where
        applicable.
      relationshipContext: Its meaning is specific to BankAccount; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        PENDING: Represents the pending state or classification in the context of BankAccount.
        ACTIVE: Represents the active state or classification in the context of BankAccount.
        BLOCKED: Represents the blocked state or classification in the context of BankAccount.
        CLOSED: Represents the closed state or classification in the context of BankAccount.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: owner
    target: Party
    cardinality: 1..*
    ownership: reference
    help:
      summary: Connects BankAccount to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a BankAccount.
      cardinalityMeaning: The declared cardinality 1..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: institution
    target: Organization
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects BankAccount to Organization so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Organization records associated with a BankAccount.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: transactions
    target: BankTransaction
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects BankAccount to BankTransaction so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about BankTransaction records associated with a BankAccount.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  help:
    summary: Represents a financial entity called BankAccount within the CEDM business model.
    businessMeaning: BankAccount is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate BankAccount records.
    relationshipContext: The entity participates in a wider business graph through relationships with Party, Organization,
      BankTransaction. These relationships provide the context needed to interpret the record rather than treating its fields
      as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical BankAccount record represents one identifiable business occurrence or master-data object that can be
      referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-BANK-ACCOUNT

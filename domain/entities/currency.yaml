entity:
  name: Currency
  namespace: cedm.domain
  kind: reference_master_entity
  description: A recognized monetary currency used for financial amounts and transactions.
  identity:
    key: currencyId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: currencyId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the currency in which monetary amounts on the record are expressed, allowing amounts to be interpreted
        and aggregated consistently.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Currency records, where applicable.
      relationshipContext: Its meaning is specific to Currency; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: code
    type: string
    required: true
    unique: true
    maxLength: 3
    help:
      summary: A human-readable business code used to identify or reference the record in operational processes and integrations.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Currency records, where applicable.
      relationshipContext: Its meaning is specific to Currency; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: name
    type: string
    required: true
    maxLength: 100
    help:
      summary: The human-readable name used by people, reports, searches, and related business processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Currency records, where applicable.
      relationshipContext: Its meaning is specific to Currency; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: symbol
    type: string
    required: false
    maxLength: 10
    help:
      summary: Captures the business meaning of symbol for the Currency. It is interpreted together with the entity's other
        attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Currency records, where applicable.
      relationshipContext: Its meaning is specific to Currency; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: decimalPlaces
    type: integer
    required: true
    default: 2
    minimum: 0
    maximum: 6
    help:
      summary: Captures the business meaning of decimal places for the Currency. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Currency records, where applicable.
      relationshipContext: Its meaning is specific to Currency; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - ACTIVE
    - INACTIVE
    - RETIRED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Currency records, where applicable.
      relationshipContext: Its meaning is specific to Currency; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        ACTIVE: Represents the active state or classification in the context of Currency.
        INACTIVE: Represents the inactive state or classification in the context of Currency.
        RETIRED: Represents the retired state or classification in the context of Currency.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  invariants:
  - id: CURRENCY-001
    rule: code must conform to the ISO 4217 three-letter currency-code convention where an ISO code exists.
  - id: CURRENCY-002
    rule: decimalPlaces must be non-negative.
  help:
    summary: Represents a reference master entity called Currency within the CEDM business model.
    businessMeaning: Currency is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate Currency records.
    relationshipContext: The entity participates in a wider business graph through relationships with other CEDM entities.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical Currency record represents one identifiable business occurrence or master-data object that can be referenced
      by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-CURRENCY

entity:
  name: PurchaseRequisitionLine
  namespace: cedm.domain
  kind: transaction_line
  description: A requested item or service on a purchase requisition.
  identity:
    key: purchaseRequisitionLineId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: purchaseRequisitionLineId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the purchase requisition line record associated with this PurchaseRequisitionLine. The identifier
        connects this record to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseRequisitionLine records,
        where applicable.
      relationshipContext: Its meaning is specific to PurchaseRequisitionLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: lineNumber
    type: integer
    required: true
    minimum: 1
    help:
      summary: Captures the business meaning of line number for the PurchaseRequisitionLine. It is interpreted together with
        the entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseRequisitionLine records,
        where applicable.
      relationshipContext: Its meaning is specific to PurchaseRequisitionLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: quantity
    type: decimal
    required: true
    precision: 20
    scale: 6
    help:
      summary: The amount of the referenced item expressed in the applicable unit of measure. It is used by calculations,
        inventory, planning, fulfillment, or other quantity-based processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseRequisitionLine records,
        where applicable.
      relationshipContext: Its meaning is specific to PurchaseRequisitionLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: requestedDate
    type: date
    required: false
    help:
      summary: Records the business date associated with the requested. It is used in chronology, eligibility, scheduling,
        reporting, and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseRequisitionLine records,
        where applicable.
      relationshipContext: Its meaning is specific to PurchaseRequisitionLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: description
    type: string
    required: true
    maxLength: 1000
    help:
      summary: A business description that explains the purpose, scope, or meaning of the record to users and downstream processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating PurchaseRequisitionLine records,
        where applicable.
      relationshipContext: Its meaning is specific to PurchaseRequisitionLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: requisition
    target: PurchaseRequisition
    cardinality: '1'
    ownership: reference
    inverse: lines
    help:
      summary: Connects PurchaseRequisitionLine to PurchaseRequisition so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about PurchaseRequisition records associated with a PurchaseRequisitionLine.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: product
    target: Product
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects PurchaseRequisitionLine to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a PurchaseRequisitionLine.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: unitOfMeasure
    target: UnitOfMeasure
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects PurchaseRequisitionLine to UnitOfMeasure so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about UnitOfMeasure records associated with a PurchaseRequisitionLine.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: PR-LINE-001
    rule: quantity must be greater than zero.
  - id: PR-LINE-002
    rule: lineNumber must be unique within a requisition.
  help:
    summary: Represents a transaction line called PurchaseRequisitionLine within the CEDM business model.
    businessMeaning: PurchaseRequisitionLine is a business concept with its own identity and lifecycle. It captures information
      that must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate PurchaseRequisitionLine records.
    relationshipContext: The entity participates in a wider business graph through relationships with PurchaseRequisition,
      Product, UnitOfMeasure. These relationships provide the context needed to interpret the record rather than treating
      its fields as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical PurchaseRequisitionLine record represents one identifiable business occurrence or master-data object
      that can be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-PURCHASE-REQUISITION-LINE

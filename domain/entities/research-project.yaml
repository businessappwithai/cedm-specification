entity:
  name: ResearchProject
  namespace: cedm.domain
  kind: scientific_research_entity
  description: A structured scientific research initiative with defined objectives, resources, experiments, and outcomes.
  identity:
    key: researchProjectId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: researchProjectId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the research project record associated with this ResearchProject. The identifier connects this record
        to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ResearchProject records, where
        applicable.
      relationshipContext: Its meaning is specific to ResearchProject; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: projectCode
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of project code for the ResearchProject. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ResearchProject records, where
        applicable.
      relationshipContext: Its meaning is specific to ResearchProject; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: title
    type: string
    required: true
    maxLength: 500
    help:
      summary: Captures the business meaning of title for the ResearchProject. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ResearchProject records, where
        applicable.
      relationshipContext: Its meaning is specific to ResearchProject; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: objective
    type: string
    required: false
    maxLength: 4000
    help:
      summary: Captures the business meaning of objective for the ResearchProject. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ResearchProject records, where
        applicable.
      relationshipContext: Its meaning is specific to ResearchProject; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: status
    type: enum
    required: true
    default: PLANNED
    values:
    - PLANNED
    - ACTIVE
    - COMPLETED
    - SUSPENDED
    - CANCELLED
    - ARCHIVED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating ResearchProject records, where
        applicable.
      relationshipContext: Its meaning is specific to ResearchProject; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        PLANNED: Represents the planned state or classification in the context of ResearchProject.
        ACTIVE: Represents the active state or classification in the context of ResearchProject.
        COMPLETED: Represents the completed state or classification in the context of ResearchProject.
        SUSPENDED: Represents the suspended state or classification in the context of ResearchProject.
        CANCELLED: Represents the cancelled state or classification in the context of ResearchProject.
        ARCHIVED: Represents the archived state or classification in the context of ResearchProject.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: organization
    target: Organization
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects ResearchProject to Organization so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Organization records associated with a ResearchProject.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: experiments
    target: Experiment
    cardinality: 0..*
    ownership: aggregate
    help:
      summary: Connects ResearchProject to Experiment so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Experiment records associated with a ResearchProject.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: documents
    target: Document
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects ResearchProject to Document so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Document records associated with a ResearchProject.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: team
    target: Party
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects ResearchProject to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a ResearchProject.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  help:
    summary: Represents a scientific research entity called ResearchProject within the CEDM business model.
    businessMeaning: ResearchProject is a business concept with its own identity and lifecycle. It captures information that
      must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate ResearchProject records.
    relationshipContext: The entity participates in a wider business graph through relationships with Organization, Experiment,
      Document, Party. These relationships provide the context needed to interpret the record rather than treating its fields
      as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical ResearchProject record represents one identifiable business occurrence or master-data object that can
      be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-RESEARCH-PROJECT

entity:
  name: Container
  namespace: cedm.domain
  kind: logistics_asset
  description: A reusable intermodal freight container tracked as a physical logistics asset.
  identity:
    key: containerId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: containerId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the container record associated with this Container. The identifier connects this record to the
        corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: containerNumber
    type: string
    required: true
    unique: true
    maxLength: 20
    help:
      summary: Captures the business meaning of container number for the Container. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: isoCode
    type: string
    required: false
    maxLength: 10
    help:
      summary: Indicates whether the iso code condition applies to this Container. Business rules and user interfaces use
        this value to determine applicable behavior.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: sizeCode
    type: string
    required: false
    maxLength: 20
    help:
      summary: Captures the business meaning of size code for the Container. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: containerType
    type: string
    required: false
    maxLength: 50
    help:
      summary: Captures the business meaning of container type for the Container. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: tareWeight
    type: decimal
    required: false
    precision: 14
    scale: 3
    help:
      summary: Captures the business meaning of tare weight for the Container. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: maxGrossWeight
    type: decimal
    required: false
    precision: 14
    scale: 3
    help:
      summary: Captures the business meaning of max gross weight for the Container. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: manufactureDate
    type: date
    required: false
    help:
      summary: Records the business date associated with the manufacture. It is used in chronology, eligibility, scheduling,
        reporting, and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - ACTIVE
    - IN_REPAIR
    - DAMAGED
    - SOLD
    - SCRAPPED
    - LOST
    - RETIRED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      valueSemantics:
        ACTIVE: Represents the active state or classification in the context of Container.
        IN_REPAIR: Represents the in repair state or classification in the context of Container.
        DAMAGED: Represents the damaged state or classification in the context of Container.
        SOLD: Represents the sold state or classification in the context of Container.
        SCRAPPED: Represents the scrapped state or classification in the context of Container.
        LOST: Represents the lost state or classification in the context of Container.
        RETIRED: Represents the retired state or classification in the context of Container.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: inventoryStatus
    type: string
    required: false
    maxLength: 50
    help:
      summary: Captures the business meaning of inventory status for the Container. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Container records, where applicable.
      relationshipContext: Its meaning is specific to Container; it must be interpreted with the entity's relationships, lifecycle,
        and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: owner
    target: Party
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Container to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a Container.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: currentLocation
    target: Location
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Container to Location so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Location records associated with a Container.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: currentSlot
    target: YardSlot
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects Container to YardSlot so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about YardSlot records associated with a Container.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: repairEstimates
    target: RepairEstimate
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects Container to RepairEstimate so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about RepairEstimate records associated with a Container.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: movements
    target: ContainerMovement
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects Container to ContainerMovement so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about ContainerMovement records associated with a Container.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: CONTAINER-001
    rule: containerNumber must follow the applicable ISO 6346 identification convention when an ISO identifier is assigned.
  - id: CONTAINER-002
    rule: A container can occupy at most one active yard slot at a time.
  help:
    summary: Represents a logistics asset called Container within the CEDM business model.
    businessMeaning: Container is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate Container records.
    relationshipContext: The entity participates in a wider business graph through relationships with Party, Location, YardSlot,
      RepairEstimate, ContainerMovement. These relationships provide the context needed to interpret the record rather than
      treating its fields as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical Container record represents one identifiable business occurrence or master-data object that can be
      referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-CONTAINER

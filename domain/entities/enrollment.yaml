entity:
  name: Enrollment
  namespace: cedm.domain
  kind: education_transaction
  description: A student's formal registration in an educational program or course.
  identity:
    key: enrollmentId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: enrollmentId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the enrollment record associated with this Enrollment. The identifier connects this record to the
        corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Enrollment records, where
        applicable.
      relationshipContext: Its meaning is specific to Enrollment; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: enrollmentNumber
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of enrollment number for the Enrollment. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Enrollment records, where
        applicable.
      relationshipContext: Its meaning is specific to Enrollment; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: enrolledAt
    type: datetime
    required: true
    help:
      summary: Records when the enrolled event occurred. It establishes chronology, supports auditability, and helps coordinate
        related lifecycle and process activities.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Enrollment records, where
        applicable.
      relationshipContext: Its meaning is specific to Enrollment; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: status
    type: enum
    required: true
    default: ACTIVE
    values:
    - PENDING
    - ACTIVE
    - COMPLETED
    - WITHDRAWN
    - CANCELLED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating Enrollment records, where
        applicable.
      relationshipContext: Its meaning is specific to Enrollment; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        PENDING: Represents the pending state or classification in the context of Enrollment.
        ACTIVE: Represents the active state or classification in the context of Enrollment.
        COMPLETED: Represents the completed state or classification in the context of Enrollment.
        WITHDRAWN: Represents the withdrawn state or classification in the context of Enrollment.
        CANCELLED: Represents the cancelled state or classification in the context of Enrollment.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: student
    target: EducationStudent
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects Enrollment to EducationStudent so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about EducationStudent records associated with a Enrollment.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: program
    target: EducationProgram
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects Enrollment to EducationProgram so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about EducationProgram records associated with a Enrollment.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: ENROLLMENT-001
    rule: a COMPLETED enrollment must have completion evidence or outcome where required by the program.
  help:
    summary: Represents a education transaction called Enrollment within the CEDM business model.
    businessMeaning: Enrollment is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate Enrollment records.
    relationshipContext: The entity participates in a wider business graph through relationships with EducationStudent, EducationProgram.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical Enrollment record represents one identifiable business occurrence or master-data object that can be
      referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-ENROLLMENT

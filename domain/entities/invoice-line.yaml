entity:
  name: InvoiceLine
  namespace: cedm.domain
  kind: financial_transaction_line
  description: A billed product, service, quantity, tax, or adjustment line on an invoice.
  identity:
    key: invoiceLineId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: invoiceLineId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the invoice line record associated with this InvoiceLine. The identifier connects this record to
        the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InvoiceLine records, where
        applicable.
      relationshipContext: Its meaning is specific to InvoiceLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: lineNumber
    type: integer
    required: true
    minimum: 1
    help:
      summary: Captures the business meaning of line number for the InvoiceLine. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InvoiceLine records, where
        applicable.
      relationshipContext: Its meaning is specific to InvoiceLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: quantity
    type: decimal
    required: false
    precision: 20
    scale: 6
    help:
      summary: The amount of the referenced item expressed in the applicable unit of measure. It is used by calculations,
        inventory, planning, fulfillment, or other quantity-based processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InvoiceLine records, where
        applicable.
      relationshipContext: Its meaning is specific to InvoiceLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: unitPrice
    type: decimal
    required: false
    precision: 20
    scale: 4
    help:
      summary: Captures the business meaning of unit price for the InvoiceLine. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InvoiceLine records, where
        applicable.
      relationshipContext: Its meaning is specific to InvoiceLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: taxAmount
    type: decimal
    required: true
    default: 0
    precision: 20
    scale: 4
    help:
      summary: Captures the business meaning of tax amount for the InvoiceLine. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InvoiceLine records, where
        applicable.
      relationshipContext: Its meaning is specific to InvoiceLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: lineAmount
    type: decimal
    required: true
    precision: 20
    scale: 4
    help:
      summary: Captures the business meaning of line amount for the InvoiceLine. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InvoiceLine records, where
        applicable.
      relationshipContext: Its meaning is specific to InvoiceLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: description
    type: string
    required: false
    maxLength: 1000
    help:
      summary: A business description that explains the purpose, scope, or meaning of the record to users and downstream processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InvoiceLine records, where
        applicable.
      relationshipContext: Its meaning is specific to InvoiceLine; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: invoice
    target: Invoice
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects InvoiceLine to Invoice so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Invoice records associated with a InvoiceLine.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: product
    target: Product
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects InvoiceLine to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a InvoiceLine.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: unitOfMeasure
    target: UnitOfMeasure
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects InvoiceLine to UnitOfMeasure so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about UnitOfMeasure records associated with a InvoiceLine.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: INVOICE-LINE-001
    rule: lineNumber must be unique within the invoice.
  help:
    summary: Represents a financial transaction line called InvoiceLine within the CEDM business model.
    businessMeaning: InvoiceLine is a business concept with its own identity and lifecycle. It captures information that must
      remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate InvoiceLine records.
    relationshipContext: The entity participates in a wider business graph through relationships with Invoice, Product, UnitOfMeasure.
      These relationships provide the context needed to interpret the record rather than treating its fields as isolated database
      columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical InvoiceLine record represents one identifiable business occurrence or master-data object that can be
      referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-INVOICE-LINE

entity:
  name: InventoryMovement
  namespace: cedm.domain
  kind: inventory_transaction
  description: A recorded movement that increases, decreases, transfers, or adjusts inventory.
  identity:
    key: inventoryMovementId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: inventoryMovementId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the inventory movement record associated with this InventoryMovement. The identifier connects this
        record to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryMovement records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryMovement; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: movementNumber
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of movement number for the InventoryMovement. It is interpreted together with
        the entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryMovement records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryMovement; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: movementType
    type: enum
    required: true
    values:
    - RECEIPT
    - ISSUE
    - TRANSFER
    - ADJUSTMENT
    - RETURN
    - RESERVATION
    - RELEASE
    help:
      summary: Captures the business meaning of movement type for the InventoryMovement. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryMovement records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryMovement; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        RECEIPT: Represents the receipt state or classification in the context of InventoryMovement.
        ISSUE: Represents the issue state or classification in the context of InventoryMovement.
        TRANSFER: Represents the transfer state or classification in the context of InventoryMovement.
        ADJUSTMENT: Represents the adjustment state or classification in the context of InventoryMovement.
        RETURN: Represents the return state or classification in the context of InventoryMovement.
        RESERVATION: Represents the reservation state or classification in the context of InventoryMovement.
        RELEASE: Represents the release state or classification in the context of InventoryMovement.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: quantity
    type: decimal
    required: true
    precision: 20
    scale: 6
    help:
      summary: The amount of the referenced item expressed in the applicable unit of measure. It is used by calculations,
        inventory, planning, fulfillment, or other quantity-based processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryMovement records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryMovement; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: movementDate
    type: datetime
    required: true
    help:
      summary: Records the business date associated with the movement. It is used in chronology, eligibility, scheduling,
        reporting, and related business rules.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryMovement records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryMovement; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: reason
    type: string
    required: false
    maxLength: 500
    help:
      summary: Captures the business meaning of reason for the InventoryMovement. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating InventoryMovement records,
        where applicable.
      relationshipContext: Its meaning is specific to InventoryMovement; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  relationships:
  - name: product
    target: Product
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects InventoryMovement to Product so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Product records associated with a InventoryMovement.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: sourceLocation
    target: InventoryLocation
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects InventoryMovement to InventoryLocation so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about InventoryLocation records associated with a InventoryMovement.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: targetLocation
    target: InventoryLocation
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects InventoryMovement to InventoryLocation so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about InventoryLocation records associated with a InventoryMovement.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: party
    target: Party
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects InventoryMovement to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a InventoryMovement.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: relatedTransaction
    target: BusinessTransaction
    cardinality: 0..1
    ownership: reference
    help:
      summary: Connects InventoryMovement to BusinessTransaction so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about BusinessTransaction records associated with a InventoryMovement.
      cardinalityMeaning: The declared cardinality 0..1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  invariants:
  - id: INVENTORY-MOVEMENT-001
    rule: quantity must be greater than zero.
  - id: INVENTORY-MOVEMENT-002
    rule: a TRANSFER requires both source and target locations.
  help:
    summary: Represents a inventory transaction called InventoryMovement within the CEDM business model.
    businessMeaning: InventoryMovement is a business concept with its own identity and lifecycle. It captures information
      that must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate InventoryMovement records.
    relationshipContext: The entity participates in a wider business graph through relationships with Product, InventoryLocation,
      InventoryLocation, Party, BusinessTransaction. These relationships provide the context needed to interpret the record
      rather than treating its fields as isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical InventoryMovement record represents one identifiable business occurrence or master-data object that
      can be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-INVENTORY-MOVEMENT

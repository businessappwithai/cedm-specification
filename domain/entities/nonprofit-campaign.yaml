entity:
  name: NonprofitCampaign
  namespace: cedm.domain
  kind: nonprofit_entity
  description: A fundraising or public-interest campaign organized to achieve a defined funding or participation objective.
  identity:
    key: campaignId
    type: uuid
    generated: true
    immutable: true
  attributes:
  - name: campaignId
    type: uuid
    required: true
    immutable: true
    help:
      summary: Identifies the campaign record associated with this NonprofitCampaign. The identifier connects this record
        to the corresponding CEDM entity so related processes can resolve the correct business object.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating NonprofitCampaign records,
        where applicable.
      relationshipContext: Its meaning is specific to NonprofitCampaign; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: campaignCode
    type: string
    required: true
    unique: true
    maxLength: 100
    help:
      summary: Captures the business meaning of campaign code for the NonprofitCampaign. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating NonprofitCampaign records,
        where applicable.
      relationshipContext: Its meaning is specific to NonprofitCampaign; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: name
    type: string
    required: true
    maxLength: 300
    help:
      summary: The human-readable name used by people, reports, searches, and related business processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating NonprofitCampaign records,
        where applicable.
      relationshipContext: Its meaning is specific to NonprofitCampaign; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  - name: objective
    type: string
    required: false
    maxLength: 2000
    help:
      summary: Captures the business meaning of objective for the NonprofitCampaign. It is interpreted together with the entity's
        other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating NonprofitCampaign records,
        where applicable.
      relationshipContext: Its meaning is specific to NonprofitCampaign; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: targetAmount
    type: decimal
    required: false
    precision: 20
    scale: 4
    help:
      summary: Captures the business meaning of target amount for the NonprofitCampaign. It is interpreted together with the
        entity's other attributes and relationships to support the processes that manage this record.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating NonprofitCampaign records,
        where applicable.
      relationshipContext: Its meaning is specific to NonprofitCampaign; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      optionalMeaning: Optional because the business concept can remain valid when this value is not yet known or is not applicable.
  - name: status
    type: enum
    required: true
    default: PLANNED
    values:
    - PLANNED
    - ACTIVE
    - COMPLETED
    - CANCELLED
    help:
      summary: The lifecycle state of the record. It controls which business actions are normally permitted and how the record
        is treated by related processes.
      usage: Used when creating, reviewing, searching, validating, reporting on, or integrating NonprofitCampaign records,
        where applicable.
      relationshipContext: Its meaning is specific to NonprofitCampaign; it must be interpreted with the entity's relationships,
        lifecycle, and business rules rather than as an isolated technical value.
      valueSemantics:
        PLANNED: Represents the planned state or classification in the context of NonprofitCampaign.
        ACTIVE: Represents the active state or classification in the context of NonprofitCampaign.
        COMPLETED: Represents the completed state or classification in the context of NonprofitCampaign.
        CANCELLED: Represents the cancelled state or classification in the context of NonprofitCampaign.
      requiredMeaning: Required because the business model cannot reliably interpret the record for its declared purpose without
        this value.
  relationships:
  - name: organization
    target: Organization
    cardinality: '1'
    ownership: reference
    help:
      summary: Connects NonprofitCampaign to Organization so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Organization records associated with a NonprofitCampaign.
      cardinalityMeaning: The declared cardinality 1 expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: donors
    target: Party
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects NonprofitCampaign to Party so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Party records associated with a NonprofitCampaign.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  - name: documents
    target: Document
    cardinality: 0..*
    ownership: reference
    help:
      summary: Connects NonprofitCampaign to Document so related business context can be navigated and enforced.
      usage: Used when processes need to find or reason about Document records associated with a NonprofitCampaign.
      cardinalityMeaning: The declared cardinality 0..* expresses how many related records may participate in the relationship.
      context: The relationship is part of the CEDM semantic graph and is interpreted together with source and target entities,
        ownership, conditions, and invariants.
  help:
    summary: Represents a nonprofit entity called NonprofitCampaign within the CEDM business model.
    businessMeaning: NonprofitCampaign is a business concept with its own identity and lifecycle. It captures information
      that must remain understandable independently of a database, API, or user interface.
    usage: Used by business processes, transactions, forms, reports, integrations, and domain capabilities that create, find,
      change, or relate NonprofitCampaign records.
    relationshipContext: The entity participates in a wider business graph through relationships with Organization, Party,
      Document. These relationships provide the context needed to interpret the record rather than treating its fields as
      isolated database columns.
    lifecycle: The entity lifecycle is governed by its status, invariants, and related business processes. State changes must
      preserve the declared business meaning and relationships.
    example: A typical NonprofitCampaign record represents one identifiable business occurrence or master-data object that
      can be referenced by related CEDM processes.
specification:
  version: 0.1.0
  status: draft
  identifier: CEDM-ENTITY-NONPROFIT-CAMPAIGN
